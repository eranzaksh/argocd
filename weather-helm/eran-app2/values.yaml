name: "web-app"
image:
  repository: eranzaksh/infinity
  tag: "latest"
service:
  type: ClusterIP
  port: 5001
spec:
  replicaCount: 1
volumes:
  name: caching
  mountPath: /home/cache
persistent:
  name: caching-pv
  storage: 1Gi
  storageClassName: ebs-gp3
ingress:
  spec:
    host: 
secret:
  key: "default"
